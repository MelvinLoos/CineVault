- The Theater:
    - Jellyfin:
        icon: jellyfin
        href: http://{{ ansible_host }}:8096
        description: Watch Movies & TV
        server: local-docker
        container: jellyfin
    - Seerr:
        icon: seerr
        href: http://{{ ansible_host }}:5055
        description: Request New Media
        server: local-docker
        container: seerr
    - Wizarr:
        icon: wizarr
        href: http://{{ ansible_host }}:5690
        description: User Invitations
        server: local-docker
        container: wizarr

- The Acquisition Pipeline:
    - The Acquisition Pipeline:
    - Radarr:
        icon: radarr
        href: http://{{ ansible_host }}:7878
        description: Movie Manager
        server: local-docker
        container: radarr
{% if radarr_key is defined and radarr_key | length > 0 %}
        widget:
            type: radarr
            url: http://radarr:7878
            key: "{{ '{{HOMEPAGE_VAR_RADARR_KEY}}' }}"
{% endif %}
    - Sonarr:
        icon: sonarr
        href: http://{{ ansible_host }}:8989
        description: TV Show Manager
        server: local-docker
        container: sonarr
{% if sonarr_key is defined and sonarr_key | length > 0 %}
        widget:
            type: sonarr
            url: http://sonarr:8989
            key: "{{ '{{HOMEPAGE_VAR_SONARR_KEY}}' }}"
{% endif %}
    - Prowlarr:
        icon: prowlarr
        href: http://{{ ansible_host }}:9696
        description: Indexer Manager
        server: local-docker
        container: prowlarr
{% if prowlarr_key is defined and prowlarr_key | length > 0 %}
        widget:
            type: prowlarr
            url: http://prowlarr:9696
            key: "{{ '{{HOMEPAGE_VAR_PROWLARR_KEY}}' }}"
{% endif %}
    - Bazarr:
        icon: bazarr
        href: http://{{ ansible_host }}:6767
        description: Subtitle Manager
        server: local-docker
        container: bazarr
{% if final_bazarr_key is defined and final_bazarr_key | length > 0 %}
        widget:
            type: bazarr
            url: http://bazarr:6767
            key: "{{ '{{HOMEPAGE_VAR_BAZARR_KEY}}' }}"
{% endif %}

    - SABnzbd:
        icon: sabnzbd
        href: http://{{ ansible_host }}:8080
        description: Usenet Downloader
        server: local-docker
        container: sabnzbd
{% if final_sabnzbd_key is defined and final_sabnzbd_key | length > 0 %}
        widget:
            type: sabnzbd
            url: http://sabnzbd:8080
            key: "{{ '{{HOMEPAGE_VAR_SABNZBD_KEY}}' }}"
{% endif %}

    - qBittorrent:
        icon: qbittorrent
        href: http://{{ ansible_host }}:8090
        description: Torrent Downloader
        server: local-docker
        container: gluetun
        widget:
            type: qbittorrent
            url: http://gluetun:8090


    - Tdarr:
        icon: tdarr
        href: http://{{ ansible_host }}:8265
        description: Media Transcoder
        server: local-docker
        container: tdarr
        widget:
            type: tdarr
            url: http://tdarr:8265


- Infrastructure:
    - Cloudflared:
        icon: cloudflare
        description: Zero Trust Tunnel
        server: local-docker
        container: cloudflared
    - Watchtower:
        icon: watchtower
        description: Auto Updater
        server: local-docker
        container: watchtower
    - Recyclarr:
        icon: recyclarr
        description: Arr Config Sync
        server: local-docker
        container: recyclarr
    - Docker Proxy:
        icon: docker
        description: Socket Proxy
        server: local-docker
        container: docker-proxy